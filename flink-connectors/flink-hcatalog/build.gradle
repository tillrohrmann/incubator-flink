
description = 'flink-hcatalog'
dependencies {
  compile project(':flink-connectors:flink-hadoop-compatibility_2.10')
    compile(group: 'org.apache.hive.hcatalog', name: 'hcatalog-core', version:'0.12.0') {
exclude(module: 'json')
    }
  providedCompile project(':flink-java')
    compile(group: 'org.scala-lang', name: 'scala-library', version:'2.10.4') {
       /* This dependency was originally in the Maven provided scope, but the project was not of type war.
       This behavior is not yet supported by Gradle, so this dependency has been converted to a compile dependency.
       Please review and delete this closure when resolved. */
    }
}
